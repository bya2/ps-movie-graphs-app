import { err_msg__prefix, msg__prefix } from "../message";

/**
 * Message
 */
export const msg__GET__movies__now_playing = `${msg__prefix}LOC: GET.movies/now_playing`;

/**
 * Error
 */
export const err_msg__GET__movies__now_playing = `${err_msg__prefix}LOC: GET.movies/now_playing`;
